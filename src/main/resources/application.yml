server:
    port: 8443
    ssl:
        key-store: build/keystore.jks
        key-store-password: secret
        key-password: secret
    
security:
  user:
    password: 'password'
    
    

logging:
    level.org.apache.http.wire: 'DEBUG'
    level.au.com.windyroad.servicegateway: 'DEBUG'
    
    
javax.net.ssl.trustStore: build/truststore.jks
javax.net.ssl.trustStorePassword: changeit

au.com.windyroad.service-gateway.proxy.max.connections:
    total: 100
    route: 5